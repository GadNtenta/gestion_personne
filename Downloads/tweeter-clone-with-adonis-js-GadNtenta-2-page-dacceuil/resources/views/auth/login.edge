<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      Connexion Twitter
    </title>
    @vite(['resources/css/app.css'])
  </head>
  <body class="bg-black min-h-screen flex flex-col items-center justify-center p-4">
    <div class="max-w-md w-full bg-black rounded-xl p-8">
      <div class="flex justify-center mb-6">
        <svg viewBox="0 0 24 24" aria-hidden="true" class="w-8 h-8 text-white fill-current">
          <g>
            <path
              d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"
            />

          </g>
        </svg>
      </div>

      <h1 class="text-white text-3xl font-bold mb-8">
        Connectez-vous
      </h1>

      @if(flashMessages.has('errors'))
        <div class="bg-red-900/50 text-red-400 p-3 rounded-md mb-4">
          <ul>
            @each(error in flashMessages.get('errors'))
              <li>
                {{ error }}
              </li>
            @end
          </ul>
        </div>
      @endif
      
      @if(flashMessages.has('error'))
        <div class="bg-red-900/50 text-red-400 p-3 rounded-md mb-4">
          {{ flashMessages.get('error') }}
        </div>
      @endif
      
      <form
        action="{{ route('auth.login') }}"
        method="POST"
        id="loginForm"
        enctype="application/x-www-form-urlencoded"
      >
        {{ csrfField() }}
        <div class="mb-4">
          <label for="email" class="block text-sm font-medium text-gray-300 mb-1">Email</label>
          <input
            type="email"
            name="email"
            id="email"
            placeholder="Email"
            value="{{ flashMessages.get('email', '') }}"
            required
            class="w-full bg-black text-white border border-gray-700 rounded-md p-4 focus:outline-none focus:border-blue-500"
          />
        </div>

        <div class="mb-6">
          <label for="password" class="block text-sm font-medium text-gray-300 mb-1">Mot de passe</label>
          <input
            type="password"
            name="password"
            id="password"
            placeholder="Mot de passe"
            required
            minlength="8"
            class="w-full bg-black text-white border border-gray-700 rounded-md p-4 focus:outline-none focus:border-blue-500"
          />
        </div>

        <button
          type="submit"
          class="w-full bg-white text-black font-bold py-3 px-4 rounded-full hover:bg-gray-200 transition"
        >
          Se connecter
        </button>
      </form>

      <script>
        document.getElementById("loginForm").addEventListener("submit", function(e) {
          const formData = new FormData(this);
          const email = formData.get("email");
          const password = formData.get("password");
          console.log("Tentative de connexion avec:", {
            email: email,
            password: password ? "[PRÉSENT]" : "[ABSENT]",
            formData: Object.fromEntries(formData)
          });
          if (!email || !password) {
            e.preventDefault();
            alert("Veuillez remplir tous les champs");
            return;
          }
        });
      </script>

      <div class="mt-6 text-center">
        <p class="text-gray-500">
          Vous n'avez pas de compte ?
        <a href="{{ route('auth.registerShow') }}" class="text-blue-500 hover:underline">Inscrivez-vous</a>
        </p>
      </div>
    </div>
  </body>
</html>

