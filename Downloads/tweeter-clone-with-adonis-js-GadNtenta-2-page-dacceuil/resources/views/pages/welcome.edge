<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome to Twitter Clone</title>
  @vite(['resources/css/app.css'])
</head>
<body class="bg-black text-white min-h-screen">
  <div class="container mx-auto px-4 py-8">
    <header class="flex justify-between items-center mb-8">
      <div>
        <svg viewBox="0 0 24 24" aria-hidden="true" class="w-8 h-8 text-white fill-current">
          <g>
            <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"></path>
          </g>
        </svg>
      </div>
      
      <div>
        @if(auth.isLoggedIn)
          <div class="flex items-center gap-4">
            <span>Welcome, {{ auth.user.fullName || auth.user.email }}</span>
            <form action="{{ route('auth.logout') }}" method="POST">
              {{ csrfField() }}
              <button type="submit" class="bg-transparent border border-gray-600 text-white px-4 py-2 rounded-full hover:bg-gray-800 transition">
                Logout
              </button>
            </form>
          </div>
        @else
          <div class="flex gap-4">
            <a href="{{ route('auth.loginShow') }}" class="bg-transparent border border-gray-600 text-white px-4 py-2 rounded-full hover:bg-gray-800 transition">
              Connexion
            </a>
            <a href="{{ route('auth.registerShow') }}" class="bg-white text-black font-bold px-4 py-2 rounded-full hover:bg-gray-200 transition">
              Inscription
            </a>
          </div>
        @endif
      </div>
    </header>
    
    <main class="flex flex-col md:flex-row gap-8 items-center justify-center py-12">
      <div class="md:w-1/2">
        <svg viewBox="0 0 24 24" aria-hidden="true" class="w-72 h-72 text-white fill-current mx-auto md:mx-0">
          <g>
            <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"></path>
          </g>
        </svg>
      </div>
      
      <div class="md:w-1/2 text-center md:text-left">
        <h1 class="text-6xl font-bold mb-12">Ça se passe maintenant</h1>
        <h2 class="text-3xl font-bold mb-8">Rejoins-nous.</h2>
        
        <div class="flex flex-col gap-4 max-w-xs mx-auto md:mx-0">
          <a href="{{ route('auth.registerShow') }}" class="bg-white text-black font-bold py-3 px-4 rounded-full hover:bg-gray-200 transition text-center">
            Créer un compte
          </a>
          
          <p class="text-gray-500 text-sm mt-2">
            Vous avez déjà un compte ?
            <a href="{{ route('auth.loginShow') }}" class="text-blue-500 hover:underline">
              Connectez-vous
            </a>
          </p>
        </div>
      </div>
    </main>
  </div>
</body>
</html>

